{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.js --minify --format=esm --bundle --outdir=src/ipyniivue_experimental/static"
	},
	"dependencies": {
		"@anywidget/types": "^0.1.5",
		"@niivue/niivue": "^0.39.0"
	},
	"devDependencies": {
		"esbuild": "^0.19.11"
	}
}
