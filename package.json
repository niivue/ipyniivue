{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.js --minify --format=esm --bundle --outdir=src/ipyniivue/static"
	},
	"dependencies": {
		"@anywidget/types": "^0.1.9",
		"@niivue/niivue": "^0.42.0"
	},
	"devDependencies": {
		"esbuild": "^0.20.2"
	}
}
